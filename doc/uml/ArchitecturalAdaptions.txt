Controllers (Diagram vs Reality):

User Service/Controller => Identity Service/Controller via AuthService, DB Entitys entsprechend angepasst
Learning Algorithm + Progress -> LearningService
PostGreSQL statt SQLite
Cookie-based authentication vs. JWT (JSON Web Tokens)

Repository Layer → Does NOT exist! Services use CoreContext directly
Entity Framework Core (DbContext) → Exists as CoreContext
Database:

JWT Authentication → Actually Cookie-based authentication